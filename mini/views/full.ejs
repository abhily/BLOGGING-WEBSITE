<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Post Display</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #1a1a1a; /* Dark background */
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column; /* Change to column to stack posts */
            height: 100vh;
            margin: 0;
            padding: 20px; /* Add padding to the body */
            color: #eee; /* Light text color */
            overflow: auto; /* Ensure the page is scrollable */
        }

        .post {
            background-color: #333; /* Darker post background */
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
            max-width: 600px;
            padding: 20px;
            margin: 20px;
            opacity: 0;
            transform: translateY(20px);
            transition: opacity 0.3s ease-out, transform 0.3s ease-out;
            overflow: auto; /* Handle overflow content */
            word-break: break-word; /* Break long words to fit within the box */
            animation: fadeInUp 0.6s ease-out forwards;
        }

        .post.show {
            opacity: 1;
            transform: translateY(0);
        }

        .post .username {
            font-weight: bold;
            color: #ffcc00; /* Username color */
            margin-bottom: 10px;
        }

        .post .postname {
            font-size: 1.2em;
            color: #ddd; /* Post name color */
            margin-bottom: 10px;
        }

        .post .content {
            color: #ccc; /* Content color */
            line-height: 1.6;
        }

        /* Custom Scrollbar Styles */
        ::-webkit-scrollbar {
            width: 12px;
        }

        ::-webkit-scrollbar-track {
            background: #1a1a1a; 
            border-radius: 6px;
        }

        ::-webkit-scrollbar-thumb {
            background: #888; 
            border-radius: 6px;
            border: 3px solid #1a1a1a;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #555; 
        }

        /* Fade-in Up Animation */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>
    <div class="post">
        <div class="username"><%= user.name %>'s post</div>
        <div class="postname"><%= post.postname %></div>
        <div class="content"><%= post.content %></div>
    </div>

    <script>
        // Optional: Add JavaScript to apply the 'show' class after some delay or on page load
        document.addEventListener('DOMContentLoaded', function() {
            var postElement = document.querySelector('.post');
            postElement.classList.add('show');
        });
    </script>
</body>
</html>
